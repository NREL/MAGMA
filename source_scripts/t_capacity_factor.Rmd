
```{r capacity_factor_table, include=TRUE}

cf = tryCatch( capacity_factor(db), error = function(cond) { return('ERROR: capacity_factor function not returning correct results.') })

kable(cf, format.args = list(big.mark = ','), digits=2)

```
