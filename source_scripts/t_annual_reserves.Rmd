
```{r annual_reserves, include=TRUE}

annual.reserves = tryCatch( annual_reserves(), error = function(cond) { return('ERROR: annual_reserves function not returning correct results.') })
kable(annual.reserves, format.args = list(big.mark = ','))

```
